# MAXXKI MLOps Produktionsdaten-Sanitizer
# =========================================
# Core (required)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# PII Anonymization (pseudonymize strategy)
faker>=24.0.0

# Synthetic Data Generation (recommended, not required)
# Falls nicht installiert → stats-basierter Fallback aktiv
sdv>=1.9.0

# Privacy mechanisms
diffprivlib>=0.6.4

# Compliance Report (optional PDF)
reportlab>=4.1.0

# MLOps Export (optional)
# mlflow>=2.12.0
# dvc>=3.49.0
